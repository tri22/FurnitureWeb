const PUBLIC_URLS = [
    "/users",

    "/users/get/{userId}",
    "/users/update/{userId}",
    "/users/delete/{userId}",

    // login url
    "/authenticate/login",
    // Product url
    "/products/all",
    "/products/{id}",
];

export default PUBLIC_URLS;
